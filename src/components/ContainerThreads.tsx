import Threads from './Threads';

export const ContainerThreads = () => {
  return (
    <article className='ContainerThreads'>
      <Threads amplitude={1.2} distance={0} enableMouseInteraction={true} />
    </article>
  );
};
